Nothing needs to be added to .gitignore since the only change is adding a single HTML file (`prompt_generator.html`) which is a source/config file and not a build artifact, dependency, or temporary file.